#!/bin/sh

# どこからでも呼び出せるようにする。
cd `git rev-parse --show-toplevel`

if [ "$OS" == 'Windows_NT' ]; then
    ASTYLE_BIN='CodeFormatter/bin/Astyle-for-Win.exe';
else
    ASTYLE_BIN='CodeFormatter/bin/astyle';
fi

${ASTYLE_BIN} --recursive ./*.cs -I --options=CodeFormatter/options/csharp.txt
